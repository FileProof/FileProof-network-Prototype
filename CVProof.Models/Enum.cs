using System;
using System.Collections.Generic;
using System.Text;

namespace CVProof.Models
{
    public enum Category { OfficialId, TempId, SupraId, ValidatorLegitimation, MiscellaneousRecursive, ProductCertificate, DeliveryTracking, Undefined, File, Text, Root, Attachment }

    public enum Role { User, Admin };
}
