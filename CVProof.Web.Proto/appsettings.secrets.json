{ // This file can be used for secret settings with keys and passwords shared by all environments
  // Below there is a short description of the current settings made in a separate files.

  // SQL Database connection string
  //"ConnectionStrings": {
  //  "DefaultConnection": "Server=YourSQLServer;Database=YourDatabaseName;User=UserName;Password=Password"
  //},

  //Jwt authentication token settings
  //"JwtKey": "Some random string used as a key to sign the json web token",
  //"JwtIssuer": "token issuer, company name",
  //"JwtExpireDays": 30, // days issued token is valid

  //Amazon S3 storage settings:
  //"keyContainerName": "String used as a name for locally stored Amazon key container",
  //"accessKey": "YourAmazonS3PublicKey",
  //"secretKey": "YourAmazonS3PrivateKey",

  //Ethereum settings
  //"ethContractAddress": "Smart contract for storing the data (anchoring)",
  //"ethAbi": "Smart contract ABI",
  //"ethSenderAddress": "EthereumAccountPublicKey",
  //"ethSenderPK": "EthereumAccountPrivateKey",
  //"ethNode": "node (e.g.infura) web address"
}